import { plans } from '@dorik/utils';
export const names = Object.keys(plans.planEnum.plan);

const advancedPageSettings = {
    default: [
        {},
    ],
};

export default advancedPageSettings;
