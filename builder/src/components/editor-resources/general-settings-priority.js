import {
    row,
    button,
    column,
    section,
    container,
} from './general-settings-elements';

const generalSettings = {
    default: [],
    row,
    column,
    button,
    section,
    container,
};

export default generalSettings;
